package Controller.UserController;

import Model.GraphPackage;

//defines control for use with the graphController class
public interface IUserController {
    public void update(GraphPackage gPack);
}
